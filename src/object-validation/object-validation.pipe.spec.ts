import { ObjectValidationPipe } from './object-validation.pipe';

describe('ObjectValidationPipe', () => {
  it('should be defined', () => {
    expect(new ObjectValidationPipe()).toBeDefined();
  });
});
